services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: agency_ops
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - agency_ops_data:/var/lib/postgresql/data
volumes:
  agency_ops_data:
